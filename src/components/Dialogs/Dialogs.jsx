import React from "react";
import s from './Dialogs.module.css';

const Dialogs = (props) => {
    return (
        <div>
            Dialogs
        </div>
    )
}

export default Dialogs; //значит что экспортируем из файла что-то главное,т.е по умолчанию